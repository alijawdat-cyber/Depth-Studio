# 📦 تحليل شامل للمشروع

1. اقرأ الملفات التالية:
   - package.json
   - firebase.json
   - firestore.rules
   - backend/src/index.ts
   - types/src/index.ts

2. استخرج المتغيرات البيئية من config وnext.config.js

3. افحص البنية التالية:
   - frontend/src/components/
   - backend/src/services/
   - types/src/core/

4. حدد الملفات التي تتجاوز 400 سطر واقترح refactor

---

# 🔧 تحسين الكود والتقسيم

1. افحص استخدام any واستبدل بـ unknown أو نوع صريح
2. نظّم كل import حسب نوعه (أنواع، خارجي، داخلي)
3. اقترح تقسيم ملفات الخدمات الكبيرة إلى:
   - validation
   - transformation
   - logic

4. أضف تعليقات عربية للملفات غير الموثقة

---

# 🚀 استعداد الإنتاج

1. افحص المتغيرات البيئية المفقودة
2. فعّل تتبع الأخطاء (Sentry)
3. فعّل مراقبة الأداء (Firebase Performance + DataDog)
4. شغّل lint وtype-check
5. تحقق من readiness للنشر على Firebase Hosting & Functions

---

# 🧪 خطة اختبار ذكي

1. أنشئ اختبارات لوحدات:
   - AuthService
   - FileUploadService
   - BaseRepository

2. شغّل اختبار تغطية:
   - `vitest --coverage`

3. أنشئ اختبار e2e كامل:
   - تسجيل دخول
   - رفع محتوى
   - الموافقة عليه

---

# 🤖 فرص الذكاء الاصطناعي

1. اقترح مكان استخدام:
   - تصنيف المحتوى بالصور
   - تحليل الجودة
   - توصية حملات
   - تسعير ديناميكي

2. أنشئ stubs لـ:
   - AIPricingEngine
   - AICampaignOptimizer
   - AIContentClassifier
